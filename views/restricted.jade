extends layout

block content
	.col-md-2
		h1= Leyndó
		p You are #{user.username}

		if users
			p Users:
			each u in users
				.list-group-item #{u.username}


	.col-md-6
		if entries
			p 
			each u in entries

				a.container-fluid(href='/addcomment/'+ u.post_id)
					.panel.panel-primary
						.panel-heading
							p#text #{u.headline}
						.panel-footer
							p#user #{u.username} 							
							p#date #{u.date}

				

	.col-md-4

